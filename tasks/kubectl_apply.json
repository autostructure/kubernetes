{
  "description": "Apply kubernetes configuration",
  "input_method": "stdin",
  "parameters": {
    "all": {
      "description": "Select all resources in the namespace of the specified resource types.",
      "type": "Optional[Boolean]"
    },
    "allow_missing_template_keys": {
      "description": "If true, ignore any errors in templates when a field or map key is missing in the template. Only applies to golang and jsonpath output formats.",
      "type": "Optional[Boolean]"
    },
    "cascade": {
      "description": "Only relevant during a prune or a force apply. If true, cascade the deletion of the resources managed by pruned or deleted resources (e.g. Pods created by a ReplicationController).",
      "type": "Optional[Boolean]"
    },
    "dry_run": {
      "description": "If true, only print the object that would be sent, without sending it.",
      "type": "Optional[Boolean]"
    },
    "filename": {
      "description": "Filename, directory, or URL to files that contains the configuration to apply",
      "type": "String[1]"
    },
    "force": {
      "description": "Delete and re-create the specified resource, when PATCH encounters conflict and has retried for 5 times.",
      "type": "Optional[Boolean]"
    },
    "grace_period": {
      "description": "Only relevant during a prune or a force apply. Period of time in seconds given to pruned or deleted resources to terminate gracefully. Ignored if negative.",
      "type": "Optional[Integer]"
    },
    "include_extended_apis": {
      "description": "If true, include definitions of new APIs via calls to the API server.",
      "type": "Optional[Boolean]"
    },
    "include_uninitialized": {
      "description": "If true, the kubectl command applies to uninitialized objects. If explicitly set to false, this flag overrides other flags that make the kubectl commands apply to uninitialized objects, e.g., \"--all\".",
      "type": "Optional[Boolean]"
    },
    "no_headers": {
      "description": "When using the default or custom-column output format, don't print headers (default print headers).",
      "type": "Optional[Boolean]"
    },
    "openapi_validation": {
      "description": "kubernetes specific config file.",
      "type": "Optional[Boolean]"
    },
    "output": {
      "description": "kubernetes specific config file.",
      "type": "Optional[String[1]]"
    },
    "overwrite": {
      "description": "kubernetes specific config file.",
      "type": "Optional[Boolean]"
    },
    "prune": {
      "description": "kubernetes specific config file.",
      "type": "Optional[Boolean]"
    },
    "prune_whitelist": {
      "description": "kubernetes specific config file.",
      "type": "Optional[String[1]]"
    },
    "record": {
      "description": "kubernetes specific config file.",
      "type": "Optional[Boolean]"
    },
    "recursive": {
      "description": "kubernetes specific config file.",
      "type": "Optional[Boolean]"
    },
    "selector": {
      "description": "kubernetes specific config file.",
      "type": "Optional[String[1]]"
    },
    "show_all": {
      "description": "kubernetes specific config file.",
      "type": "Optional[Boolean]"
    },
    "show_labels": {
      "description": "kubernetes specific config file.",
      "type": "Optional[Boolean]"
    },
    "sort_by": {
      "description": "kubernetes specific config file.",
      "type": "Optional[String[1]]"
    },
    "template": {
      "description": "kubernetes specific config file.",
      "type": "Optional[String[1]]"
    },
    "timeout": {
      "description": "kubernetes specific config file.",
      "type": "Optional[Integer]"
    },
    "validate": {
      "description": "kubernetes specific config file.",
      "type": "Optional[Boolean]"
    }
  }
}
